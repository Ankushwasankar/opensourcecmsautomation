package com.csg.tau.prj.opensourcecms;

import org.slf4j.LoggerFactory;
import org.openqa.selenium.WebDriver;
import org.slf4j.Logger;

/*import static com.csg.tau.prj.opensourcecms.PropertyKeys.PROPERTY_BROWSER;*/

public class LoginScreenFactory {
/*

    public static final Logger Log = LoggerFactory.getLogger(LoginScreenFactory.class);

    public static LoginScreen create(ConfigProvider configProvider,WebDriver wd){

        String browserType =configProvider.getRequired(PROPERTY_BROWSER);

        Log.debug("Creating native LoginScreen for platfrom{},",browserType);

        if(LoginScreenBrowser.supportBrowserType(browserType)){
            return new LoginScreenBrowser(wd,configProvider);
        }else{
            throw new TAConfigException(PROPERTY_BROWSER+":"+browserType+"value is not supported in LoginScreenFactory");
        }
    }
*/

}
