package com.csg.tau.prj.opensourcecms.po;
//import ch.racic.testing.cm.ConfigProvider;
/*
import org.jfree.util.Log;
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.testng.Assert;*/

import com.csg.tau.prj.opensourcecms.BaseMobilePageMethods;
import com.csg.tau.prj.opensourcecms.utils.MessageResource;
/*import sun.rmi.runtime.Log;*/

/*import static com.csg.tau.prj.opensourcecms.utils.MessageConstant.*;*/

public class CustomerPO extends BaseMobilePageMethods {

/*    public CustomerPO(WebDriver wd, ConfigProvider configProvider){
        super (wd, configProvider);
    }

    public void enterAccounthumber (String accountNumber) {
        waitTillPageLoad ();
        if(isWebElementPresent(Element.AccountNumber.locator)){
            getElementsafely (Element.AccountNumber.locator).sendKeys(accountNumber);
            getElementsafely (E1ement.SearcnButton.loocator).click();
            Assert.assertEquals(readText(Element.LoanDetatails.locator),MessageResource.getMessage(IPRICE_LOAN_DETAILS),"Verify loan detail page displayed.");
        }else{

            Log.info("No data available with provided Account Number.");
        }
    }*/

/*    private enum Element{

        AccountNumber(By.xpath("")),
        LoanDetatails(By.xpath("")),
        CLientSearch(By.xpath(""));

        private final By locator;

        Element(By locator){

            this.locator=locator;}
    }*/
}

